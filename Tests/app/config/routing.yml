project_cookie_read:
    path: cookie/read
    defaults:
        _controller: project.controller.cookie:readAction

project_cookie_update:
    path: cookie/update
    defaults:
        _controller: project.controller.cookie:updateAction

project_cookie_clear:
    path: cookie/clear
    defaults:
        _controller: project.controller.cookie:clearAction
